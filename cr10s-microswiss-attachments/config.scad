function inch(x) = x * 25.4;

screw_hole_center_to_center = inch(0.55125);
screw_clearance_diameter = 3.4;
screw_head_diameter = 5.5;

boss_cutout_length = inch(0.8);
boss_cutout_height = inch(0.225);
boss_cutout_depth = inch(0.17);

backplate_height = boss_cutout_height + 20 + 23;
backplate_width = inch(1);
backplate_thickness = boss_cutout_depth + inch(0.2);
backplate_padding = 2;
